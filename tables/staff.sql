CREATE TABLE staff(
    staff_id INT,
    first_name VARCHAR2(50) NOT NULL,
    last_name VARCHAR2(50) NOT NULL,
    birth_date DATE NOT NULL,
    address_id INT,
    blood_bank_id INT,
    position_id INT
);